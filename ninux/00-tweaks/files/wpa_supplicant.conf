ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1

# country= and the network block for ${WPA_ESSID} are appended by 01-run.sh
# from WPA_COUNTRY / WPA_ESSID / WPA_PASSWORD -- do not repeat them here.

network={
	ssid="ninux.org"
	key_mgmt=NONE
}
